import type { Metadata } from "next";

import { ComingSoon } from "@/components/coming-soon";

export const metadata: Metadata = {
  title: "إنشاء دعوة جديدة — Flower & Love",
};

export default function NewInvitationPage() {
  return <ComingSoon />;
}
